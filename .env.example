# Copy to .env (gitignored) and fill in. Loaded automatically at startup.

# Claude API key. When set, Architect.IQ uses the LLM for requirement extraction,
# capability derivation, pattern matching, and image (vision) ingest. Without it,
# the engine falls back to deterministic heuristics and still runs fully.
ANTHROPIC_API_KEY=

# Optional: override the model used for LLM ingest/matching.
# FATHOM_LLM_MODEL=claude-sonnet-5

# Optional: force the deterministic path even when a key is present (1/true/yes).
# FATHOM_DISABLE_LLM=

# Optional: SQLite database path (default: architect_iq.db in the working dir).
# FATHOM_DB=architect_iq.db

# Optional: allowed CORS origins for the API (comma-separated).
# FATHOM_CORS=http://localhost:5173

# REQUIRED IN PRODUCTION: JWT signing secret (32+ bytes). A dev fallback is used
# if unset, with a warning.
# FATHOM_SECRET=

# Override the seeded admin login (defaults: admin@architect.iq / admin123).
# FATHOM_ADMIN_EMAIL=
# FATHOM_ADMIN_PASSWORD=

# Google SSO (OIDC). When set, the login screen offers "Sign in with Google".
# FATHOM_REDIRECT default: http://localhost:8000/api/auth/google/callback
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=

# Notion API key for live opportunity notes (otherwise modeled sample notes).
# NOTION_API_KEY=
